<!DOCTYPE html>
<html lang="en">

<head>
  <%- include("./partials/head") %>
  <link rel="stylesheet" href="/css/productList.css" />
</head>

<body>
  <%- include("./partials/header") %>
  <main class="main">
    <div class="main__container">
      <h1>Listado de productos:</h1>
      <section class="main__product-list">
        <% product.forEach(element => { %>
        <a href="/product/detail/<%= element.id %>">
          <article class="main__product-article">
            <div class="main__img-container">
              <img src="/images/productDetail/<%= element.productImage ? element.productImage : "Logo.png"%>" alt="Imágen de producto">
            </div>
            <div class="main__product-information">
              <p>Información del producto:</p>
              <ul>
                <li>Nombre: <%= element.name %> </li>
                <li>Tipo: <%= element.type %></li>
                <li>Duración: <%= element.length %></li>
                <li>Clasificación: <%= element.rated %> </li>
                <li>Venta: $<%= element.purchasePrice %> </li>
                <li>Alquiler: $<%= element.rentalPrice %> </li>
              </ul>
            </div>
          </article>
        </a>
        <% }) %>
      </section>
    </div>
  </main>
  <%- include("./partials/footer") %>
</body>

</html>